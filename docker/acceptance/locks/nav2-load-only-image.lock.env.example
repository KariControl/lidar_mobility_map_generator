# Replace the placeholder with the immutable digest of a locally available
# Nav2 Jazzy image. The load-only runner never pulls a missing image.
LMMG_NAV2_ACCEPTANCE_IMAGE=registry.example/lmmg-nav2-jazzy@sha256:<64-hex-digest>
